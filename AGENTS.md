# 幼升小趣味闯关 - 需求拆解文档

## 产品概述

- **产品类型**: 儿童答题闯关游戏
- **场景类型**: <scene_type>prototype-app</scene_type>
- **游戏类型**: 答题游戏（益智教育类）
- **目标用户**: 幼儿园大班/幼升小阶段儿童（5-6岁）
- **核心价值**: 通过卡通糖果风格的趣味答题闯关，帮助幼儿在游戏中巩固语文古诗、数学、拼音知识
- **界面语言**: 中文
- **主题偏好**: 浅色（卡通糖果风格，天空草地渐变背景）
- **导航模式**: 无导航（单页应用，通过状态切换模拟页面跳转）

---

## 游戏状态机

| 状态 | 说明 | UI 展示 | 触发转换 |
|------|------|---------|---------|
| `home` | 首页/科目选择 | 三个科目卡片（语文古诗、数学乐园、拼音练习），卡片浮动动画，天空草地背景 | 点击卡片 → `playing`（传入科目） |
| `playing` | 答题中 | 顶部返回按钮+科目标签+星星进度条，题目气泡卡片，2x2选项网格，每题有🔊播放按钮 | 答完10题 → `result`；点击返回 → `home` |
| `result` | 结果页 | 得分展示（答对X/10），星星评价（1-5星），撒花特效（全对时），再来一次/换科目按钮 | 再来一次 → `playing`（同科目）；换科目 → `home` |
| `wrongbook` | 错题本 | 错题列表，每题可点击重新练习，空状态提示 | 点击错题 → `playing`（单题练习模式）；返回 → `home` |

---

## 页面结构

> **说明**：本应用为单页HTML，通过状态切换实现4个视图，无路由跳转

**页面文件**: `index.html`（纯HTML单文件，内联CSS和JS）

| 视图/区块 | 说明 | 包含元素 |
|-----------|------|---------|
| 首页视图 (`home`) | 科目选择入口 | 天空草地渐变背景、飘动白云、太阳、三个科目卡片（浮动动画）、错题本入口按钮 |
| 答题视图 (`playing`) | 答题主界面 | 顶部栏（返回按钮+科目标签+星星进度条）、题目气泡卡片、🔊播放按钮、2x2选项网格（每个选项行含独立🔊喇叭+选项文字按钮） |
| 结果视图 (`result`) | 得分展示 | 得分大字展示、星星评价（1-5星）、撒花特效（全对时）、再来一次按钮、换科目按钮 |
| 错题本视图 (`wrongbook`) | 错题复习 | 错题列表（题目+正确答案+用户错误答案）、空状态提示、返回首页按钮 |

---

## 核心机制

### 游戏规则
- **目标**: 从10道随机抽取的题目中尽可能多答对
- **题目来源**: 三个科目各有题库（≥10道），每次随机抽取10道
- **操作方式**: 点击选项按钮作答，点击🔊喇叭按钮播放对应语音
- **计分规则**: 答对+1分，答错+0分，满分10分
- **星星评价**: 0-2对=1星，3-4对=2星，5-6对=3星，7-8对=4星，9-10对=5星
- **错题收录**: 答错的题目自动存入localStorage错题本
- **撒花特效**: 10题全对时触发撒花动画

### 交互反馈
| 操作 | 反馈 |
|------|------|
| 点击科目卡片 | 卡片按压动画 → 切换到答题视图 |
| 点击🔊喇叭（题目） | 语音播报题目内容（Web Speech API，强制普通话） |
| 点击🔊喇叭（选项） | 语音播报该选项文字（Web Speech API，强制普通话） |
| 点击选项按钮（答对） | 按钮变绿+弹跳动画+正确音效+星星进度+1 |
| 点击选项按钮（答错） | 按钮变红+抖动动画+错误音效+正确答案高亮 |
| 全部答对 | 结果页撒花特效（彩色纸屑飘落） |
| 点击再来一次 | 重新随机抽题，进入答题视图 |
| 点击换科目 | 返回首页视图 |

---

## 数据来源声明

| 数据/操作 | 来源类型 | 实现要求 | mock 兜底 |
|---|---|---|---|
| 题库数据（语文古诗/数学乐园/拼音练习） | demo-mock | 在JS中定义三个题库常量数组，每个≥10道题，题目结构含题干、选项、正确答案、科目 | ✅ 本身就是mock |
| 语音播报（题目和选项） | real-api | 使用浏览器Web Speech API (`window.speechSynthesis`)，强制选择zh-CN普通话语音，排除粤语/香港语音 | 无（浏览器原生API，不可用时静默降级） |
| 错题本存储 | local-persist | localStorage key=`__game_yxxq_wrongbook`，存储错题数组（含题目、正确答案、用户答案、科目、时间戳） | 无（初始为空数组） |
| 音效播放（答对/答错） | demo-mock | 使用Web Audio API生成简单提示音（正确音高/错误音低），无需外部音频文件 | ✅ 本身就是mock |

---

## 功能列表

### 首页视图 (`home`)
- **页面目标**: 让小朋友选择想挑战的科目
- **功能点**:
  - **科目卡片展示**: 三个圆角卡片（语文古诗📜、数学乐园🧮、拼音练习🔤），带浮动动画，卡片有白色边框+底部彩色阴影立体效果
  - **科目选择**: 点击卡片进入对应科目的答题视图，随机抽取10道题
  - **错题本入口**: 首页底部/角落放置错题本按钮（📒图标），点击进入错题本视图
  - **背景动画**: 天空草地渐变背景，CSS动画实现白云飘动和太阳光晕

### 答题视图 (`playing`)
- **页面目标**: 完成10道随机题目的作答
- **功能点**:
  - **顶部信息栏**: 左侧返回按钮（←），中间科目标签（如"语文古诗"），右侧星星进度条（当前进度/10，用⭐表示已完成）
  - **题目展示**: 气泡卡片样式展示题干文字，卡片上方有🔊喇叭按钮可播报题目
  - **选项网格**: 2x2网格布局，每个选项行包含独立的🔊喇叭按钮（左侧小按钮）+ 选项文字按钮（右侧大按钮），喇叭和选项分开防止误触
  - **答题交互**: 点击选项后判断对错，答对绿色弹跳+正确音效，答错红色抖动+错误音效+显示正确答案，自动进入下一题（延迟1.5秒）
  - **语音播报**: 使用Web Speech API播报题目和选项，强制选择zh-CN普通话语音（排除粤语/香港语音），`getZhVoice()`函数实现精确匹配逻辑

### 结果视图 (`result`)
- **页面目标**: 展示本次答题成绩，鼓励小朋友
- **功能点**:
  - **得分展示**: 大字号显示"答对 X / 10"，配合动画数字滚动效果
  - **星星评价**: 根据得分显示1-5颗⭐，带放大弹出动画
  - **撒花特效**: 10题全对时触发全屏彩色纸屑飘落动画（CSS动画+JS动态生成纸屑元素）
  - **操作按钮**: "再来一次"按钮（同科目重新挑战）、"换科目"按钮（返回首页），按钮带立体效果

### 错题本视图 (`wrongbook`)
- **页面目标**: 查看和复习做错的题目
- **功能点**:
  - **错题列表**: 展示所有错题，每条显示题目文字、正确答案（绿色高亮）、用户错误答案（红色划线）、所属科目、作答时间
  - **重新练习**: 点击某道错题可进入单题练习模式（仅展示该题，答对后返回错题本并从列表中移除）
  - **空状态**: 无错题时显示"太棒了！没有错题 🎉"的鼓励文案
  - **返回按钮**: 顶部返回按钮回到首页

---

## 数据存储

| 存储键名 | 数据说明 |
|---------|---------|
| `__game_yxxq_wrongbook` | 错题本数据，类型为 `IWrongQuestion[]` |

```ts
interface IWrongQuestion {
  /** 错题唯一ID */
  id: string;
  /** 所属科目 */
  subject: 'chinese' | 'math' | 'pinyin';
  /** 题干文字 */
  question: string;
  /** 正确选项文字 */
  correctAnswer: string;
  /** 用户选择的错误选项文字 */
  userAnswer: string;
  /** 所有选项列表 */
  options: string[];
  /** 作答时间戳 */
  timestamp: number;
}
```

---

## 题库结构

```ts
interface IQuestion {
  /** 题目唯一ID */
  id: string;
  /** 所属科目 */
  subject: 'chinese' | 'math' | 'pinyin';
  /** 题干文字 */
  question: string;
  /** 选项列表（4个） */
  options: string[];
  /** 正确答案索引（0-3） */
  correctIndex: number;
}
```

### 题库示例（每科≥10道）

**语文古诗**（上下句填空）：
- "床前明月光，___" → 疑是地上霜
- "锄禾日当午，___" → 汗滴禾下土
- "鹅鹅鹅，___" → 曲项向天歌
- "春眠不觉晓，___" → 处处闻啼鸟
- "白日依山尽，___" → 黄河入海流

**数学乐园**（10以内加减法、比大小）：
- "3 + 5 = ?" → 8
- "9 - 4 = ?" → 5
- "7 和 5 谁更大？" → 7
- "2 + 6 = ?" → 8
- "10 - 3 = ?" → 7

**拼音练习**（声母韵母、拼读、声调）：
- "'bā' 的声调是？" → 第一声
- "'māo' 的声母是？" → m
- "'dà' 的韵母是？" → a
- "哪个是声母？" → b / p / m / f（选项含声母和韵母混合）

---

## 技术要求确认

- **文件结构**: 纯HTML单文件，内联`<style>`和`<script>`
- **响应式**: 移动端优先，适配手机（320px+）和iPad（768px+）
- **语音API**: `window.speechSynthesis`，`getZhVoice()`函数强制普通话
- **音效**: Web Audio API生成简单提示音，无需外部音频文件
- **存储**: `localStorage`存储错题本
- **动画**: CSS animations（卡片浮动、弹跳、抖动、撒花、星星弹出）+ JS动态生成撒花纸屑
- **兼容性**: 现代浏览器（Chrome/Safari/Edge），直接打开可用

-------

<scene_type>prototype-app</scene_type>

# UI 设计指南

## 1. 设计推导依据

- **参考意图**: Free Direction —— 无参考材料，从儿童教育产品语义与卡通糖果风格需求出发自主建立视觉系统
- **核心情绪 / 应用类型**: 幼升小趣味闯关答题游戏 —— 明亮、安全、鼓励探索的轻量互动学习体验
- **独特记忆点**: 每一块界面元素都像糖果包装纸上的立体贴纸——白色高光环边 + 底部彩色厚阴影，按钮按下时有“按压糖果”的弹性反馈

## 2. Art Direction

- **方向名**: Candy Sticker Pop
- **Design Style**: Soft Pop + Claymorphism 软陶拟物 —— 圆润Q弹的立体感天然适合儿童触屏交互，糖果色系降低答题焦虑，白色边框模拟贴纸边缘的高光
- **DNA 参数**: 圆角 pill/rounded-3xl / 阴影 layered 底部彩色厚阴影 / 间距 spacious / 字体方向 圆体卡通 / 装饰手法 天空草地渐变背景 + 飘动白云太阳 + 星星进度条 + 撒花粒子
- **应用类型**: Game / Interactive —— 全屏游戏画布，内容居中，移动端优先

## 3. Color System

**色彩关系**: 糖果粉主色 + 柠檬黄辅助 + 薄荷绿点缀 + 天空蓝渐变底 + 奶油白卡片
**配色设计理由**: primary 糖果粉承担主按钮、科目标签和正确反馈，传递温暖鼓励而非考试压力；天空渐变 bg 建立户外游乐场氛围；每个科目卡片拥有独立识别色（语文杏橙、数学薄荷绿、拼音薰衣草紫），但都共享同一套白色边框 + 底部彩色阴影的立体语言
**主色推导**: 从儿童糖果包装、绘本封面和幼儿园教具中提取——高亮度、中高饱和、暖色调主导，避免荧光刺眼
**使用比例**: 55% 天空渐变背景 / 30% 奶油白卡片与按钮 / 15% 糖果粉 primary + 科目识别色

| 角色 | CSS 变量 | Tailwind Class | HSL 值 | 设计说明 |
|---|---|---|---|---|
| bg | `--background` | `bg-background` | hsl(200 70% 92%) | 天空蓝渐变起始色，模拟晴朗天空 |
| card | `--card` | `bg-card` | hsl(45 30% 97%) | 奶油白卡片底，温暖不刺眼，承载题目气泡和选项 |
| text | `--foreground` | `text-foreground` | hsl(240 10% 20%) | 深紫灰标题和正文，保证大字号下的柔和可读 |
| textMuted | `--muted-foreground` | `text-muted-foreground` | hsl(240 5% 50%) | 进度数字、辅助说明 |
| primary | `--primary` | `bg-primary` / `text-primary` | hsl(350 75% 62%) | 糖果粉——主按钮、正确反馈、星星点亮、CTA |
| primaryForeground | `--primary-foreground` | `text-primary-foreground` | hsl(0 0% 100%) | primary 上的白色文字和图标 |
| accent | `--accent` | `bg-accent` | hsl(45 90% 65%) | 柠檬黄——hover 浅底、喇叭按钮、星星底色、选中态 |
| accentForeground | `--accent-foreground` | `text-accent-foreground` | hsl(40 80% 25%) | accent 上的深暖色文字 |
| border | `--border` | `border-border` | hsl(0 0% 100%) | 白色边框，模拟贴纸高光环边，所有按钮和卡片统一使用 |

**语义色提示**:
- 正确反馈: bg hsl(145 55% 75%) / border hsl(145 50% 55%) / text hsl(145 60% 20%)，薄荷绿，饱和度与 primary 对齐，传递“你真棒”而非警告
- 错误反馈: bg hsl(25 70% 85%) / border hsl(25 60% 60%) / text hsl(25 50% 25%)，暖杏色，避免红色带来的挫败感，符合低龄儿童心理
- 科目识别色——语文杏橙 hsl(30 80% 65%)、数学薄荷绿 hsl(160 50% 55%)、拼音薰衣草紫 hsl(270 50% 70%)，仅用于卡片头部和标签，不参与交互状态

## 4. 字体与节奏

- **font-display**: ZCOOL KuaiLe —— Google Fonts 可用，卡通粗体，天然圆润俏皮，适合幼儿园阶段的文字识别
- **font-body**: ZCOOL KuaiLe —— 全站统一，保持视觉一致性，中文覆盖完整
- **字号**: 首页科目名 text-3xl ~ text-4xl；题目 text-2xl ~ text-3xl；选项按钮 text-xl ~ text-2xl；进度 text-lg；所有文字比成人产品大 1-2 级
- **圆角**: 极端 rounded-3xl ~ rounded-full —— 按钮 pill 形，卡片 rounded-3xl，模拟糖果和贴纸的圆润边缘

## 5. 全局布局契约

- **Reference Layout Use**: 按需求结构推导——首页三卡片网格、答题页顶部导航+题目气泡+2x2 选项网格、结果页居中得分面板、错题本列表
- **Page / Section Order**: 首页 → 答题页 → 结果页 → 错题本（独立入口），与需求文档页面结构 1:1 对齐
- **Standard Content Zone**: `max-w-lg`（移动端全宽 px-4，平板居中 max-w-lg），理由：2x2 选项网格 + 大按钮需要宽度但不过度拉伸，保持触屏拇指可达
- **Shell / Frame Alignment**: 无 chrome 框架，全屏沉浸式游戏画布，内容区独立居中
- **Padding & Rhythm**: `px-4 py-6 md:px-6 md:py-8`，选项间距 gap-4，卡片间距 gap-6，保持 8px 倍数
- **Full-bleed Zones**: 天空草地渐变背景全屏 `w-full h-full`，飘动白云和太阳为固定背景层；题目气泡卡片和选项网格受 Standard Content Zone 约束
- **Local Narrowing**: 错题本列表在移动端全宽，平板收窄至 max-w-lg 内
- **Overflow Strategy**: 错题列表使用 `overflow-y-auto`，保持页面整体不滚动出视口
- **Flexibility Boundary**: 允许移动端卡片内边距从 p-6 缩至 p-4；不允许切换圆角系统、阴影语言或主色

## 6. 视觉与动效

- **装饰**: 飘动白云（CSS 动画平移）、太阳（固定右上角）、星星进度条（点亮动画）、撒花粒子（全对时触发）
- **阴影/边界**: 重阴影——所有按钮和卡片底部 4-6px 彩色阴影（颜色为按钮主色的深色变体），白色 2px 实线边框模拟贴纸高光环边
- **动效**: 丰富——首页卡片轻轻浮动（translateY 循环 ±8px）；按钮按下 scale(0.92) + 阴影收缩模拟按压糖果；答对弹跳放大 + 星星闪烁；答错左右摇晃；撒花从中心向外扩散粒子；页面切换柔和淡入

## 7. 组件原则

- 按钮五态完整：Default（白边框+彩色底+底部厚阴影）、Hover（阴影加深+轻微上浮）、Active（scale 0.92+阴影收缩至 2px）、Focus（白色外发光 ring-4 ring-white/50）、Disabled（灰度降低+阴影减弱）
- 喇叭按钮独立为 48px 圆形 accent 柠檬黄按钮，与选项文字按钮间距 gap-3，避免误触
- 选项文字按钮使用 pill 形，白边框 + 科目对应浅色底 + 底部彩色阴影
- 星星进度条：未点亮为浅灰轮廓星，点亮为 accent 柠檬黄填充 + 弹跳动画
- 加载与空状态：错题本为空时显示大颗星星插图 + “太棒了，没有错题！”鼓励文案，延续糖果贴纸风格

## 8. Image Direction

- **Image Role**: 全屏背景氛围图——天空草地渐变 + 飘动白云 + 太阳，作为所有页面的固定背景层
- **Image Art Direction**: 扁平矢量插画风格，晴朗蓝天到嫩绿草地的纵向渐变，3-4 朵圆润白云以不同速度水平飘动，右上角一颗带微笑表情的卡通太阳散发柔和光芒；整体色调明亮温暖，留出中央大面积干净区域承载题目卡片；不抢前景交互元素
- **Image Prompt Keywords**: flat vector illustration, cartoon sky gradient, rolling green hills, fluffy white clouds, smiling sun, children's book style, warm daylight, soft pastel colors, clean background, wide open sky, nursery room mural
- **Image Avoidance**: 避免写实风景照片、3D 渲染天空、过于复杂的云层细节、暗角或逆光效果、成人化的极简渐变

## 9. Anti-patterns

- **Split personality**: 科目卡片使用不同识别色但共享同一套白边框+阴影语言，不能语文用圆角卡片、数学用直角卡片
- **Phantom tokens**: 不编造 `--candy-shadow` 等非标准变量，底部彩色阴影用 box-shadow 内联或 Tailwind 自定义阴影值实现
- **Default SaaS drift**: 不能出现默认蓝 #3B82F6 按钮、灰色边框卡片、Inter 字体；所有界面必须延续糖果粉+白边框+ZCOOL KuaiLe 的视觉系统
- **Invisible interaction**: 喇叭按钮必须有明显的 hover 放大和 focus 光环，儿童需要清晰的触控反馈
- **Mono-hue tyranny**: 糖果粉只用于主 CTA 和正确反馈，科目卡片、喇叭按钮、星星进度条分别使用杏橙/薄荷绿/薰衣草紫/柠檬黄，避免全屏粉红
- **Status color drift**: 错误反馈用暖杏色而非正红色，避免给低龄儿童造成挫败感；暖杏色饱和度与 primary 糖果粉对齐，不刺眼
- **Typography regression**: 任何情况下不能退回到系统默认宋体或 Arial；ZCOOL KuaiLe 加载失败时 fallback 为 Noto Sans SC，但仍需保持大字号和圆角